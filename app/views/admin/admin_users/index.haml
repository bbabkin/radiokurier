- @page_title = "Admin Users List"
%h2 Admin Users
= link_to('добавить нового админа', {:action => 'new'}, :class => 'action new')
%div
	= pluralize(@admin_users.size, 'admin user')
	found
%table.admintable{:summary => "Admin users list"}
	%tr
		%th Username
		%th Actions
	- @admin_users.each do |admin_user|
		%tr{:class => cycle('odd', 'even')}
			%td= admin_user.username
			%td.actions
				= link_to('правка', {:action => 'edit', :id => admin_user.id}, class: 'btn btn-xs btn-default')
